# Restaurant POS

A fully self-contained POS prototype built with HTML, CSS and JavaScript.

## Deploy to Vercel

1. Upload this folder to GitHub
2. Import the repo on vercel.com
3. Vercel will auto-detect it as a static site — hit Deploy

## Local Preview

Just open `public/index.html` in any browser — no server needed.
